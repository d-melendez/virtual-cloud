from django.utils.translation import gettext_lazy as _

# Minimal, sane defaults. Copy this file to your host and mount it at
# /etc/openstack-dashboard/local_settings.py inside the container.

DEBUG = False
ALLOWED_HOSTS = ['*']  # change to your domain(s) in production
TIME_ZONE = 'UTC'
WEBROOT = '/'

# Keystone API endpoint
# OPENSTACK_KEYSTONE_URL = 'http://<keystone-host-or-ip>:5000/v3'

# Use in-process cache to avoid extra dependencies
CACHES = {
    'default': {
        'BACKEND': 'django.core.cache.backends.locmem.LocMemCache',
    }
}

